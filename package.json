{
  "name": "spawntree-monorepo",
  "private": true,
  "type": "module",
  "scripts": {
    "dev:daemon": "node packages/daemon/dist/server-main.js",
    "dev:daemon:src": "node packages/daemon/src/server-main.ts",
    "dev:web": "pnpm --filter spawntree-web exec vite dev",
    "dev:web:qa": "cd packages/web && pnpm exec vite dev --host 127.0.0.1 --port 4217 --strictPort",
    "build": "pnpm --filter spawntree-core run build && pnpm --filter spawntree-daemon run build && pnpm --filter spawntree run build && pnpm --filter spawntree-web run build:only",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "oxlint . && dprint check",
    "format": "dprint fmt",
    "effect:lsp:patch": "effect-language-service patch",
    "typecheck": "pnpm --filter spawntree-core run build && pnpm --filter spawntree-daemon run build && tsc -p packages/cli/tsconfig.json --noEmit && tsc -p packages/web/tsconfig.json --noEmit",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "pnpm build && changeset publish"
  },
  "devDependencies": {
    "@changesets/changelog-github": "^0.6.0",
    "@changesets/cli": "^2.30.0",
    "@effect/language-service": "^0.75.1",
    "@playwright/test": "^1.59.1",
    "@types/node": "^25.5.0",
    "dprint": "^0.51.1",
    "jsdom": "^29.0.1",
    "oxlint": "^1.42.0",
    "typescript": "^5.8.0",
    "vitest": "^3.1.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3",
      "dprint"
    ]
  },
  "packageManager": "pnpm@10.27.0",
  "engines": {
    "node": ">=20"
  }
}
