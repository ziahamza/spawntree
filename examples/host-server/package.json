{
  "name": "spawntree-host-server-example",
  "version": "0.0.0",
  "private": true,
  "description": "Example federation host: aggregates multiple spawntree daemons behind a single API and dashboard",
  "type": "module",
  "main": "./src/server.ts",
  "scripts": {
    "start": "node --experimental-strip-types src/server.ts",
    "dev": "node --watch --experimental-strip-types src/server.ts"
  },
  "dependencies": {
    "better-sqlite3": "^12.4.1"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.12"
  },
  "engines": {
    "node": ">=22"
  }
}
