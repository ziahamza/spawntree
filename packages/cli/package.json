{
  "name": "spawntree",
  "version": "0.7.0",
  "description": "Isolated environment orchestrator. Docker Compose, but with native processes and per-branch isolation.",
  "keywords": [
    "cli",
    "development",
    "devtools",
    "docker-compose",
    "environment",
    "orchestrator"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/ziahamza/spawntree.git",
    "directory": "packages/cli"
  },
  "bin": {
    "spawntree": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "prepublishOnly": "pnpm build"
  },
  "dependencies": {
    "commander": "^13.0.0",
    "spawntree-core": "workspace:*",
    "spawntree-daemon": "workspace:*",
    "yaml": "^2.7.0"
  }
}
