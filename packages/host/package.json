{
  "name": "spawntree-host",
  "version": "0.4.0",
  "description": "Federation host for spawntree — aggregates multiple daemons behind one API and dashboard",
  "keywords": [
    "devtools",
    "federation",
    "orchestrator",
    "proxy",
    "spawntree"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/ziahamza/spawntree.git",
    "directory": "packages/host"
  },
  "bin": {
    "spawntree-host": "./dist/server.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "type": "module",
  "main": "./dist/server.js",
  "types": "./dist/server.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "node --watch --experimental-strip-types src/server.ts",
    "start": "node dist/server.js",
    "start:src": "node --experimental-strip-types src/server.ts",
    "prepublishOnly": "pnpm build"
  },
  "dependencies": {
    "better-sqlite3": "^12.4.1"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.12",
    "@types/node": "^25.5.0",
    "typescript": "^5.8.0"
  },
  "engines": {
    "node": ">=20"
  }
}
